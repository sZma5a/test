name: Dummy CI

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  dummy-ci:
    name: dummy-ci
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Simulate a long-running CI check
        run: sleep 30

      - name: Report success
        run: echo "Dummy CI completed successfully."
